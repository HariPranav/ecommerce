class User
{
  final String uid;

  // constructor
  // when we get a user id we use the this.uid to auto set the value of the final Sting uid defined at the top to a user id
  // next we need to get the object from the auth object from the auth.dart and pass it on to the user.dart
  User({this.uid});

}